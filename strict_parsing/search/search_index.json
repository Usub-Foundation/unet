{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"unet Documentation","text":"<p>Disclaimer: Due to life constraints, I could not write this documentation myself. This documentation was generated from the current repository state. We will review this documentation later.</p> <p><code>unet</code> is a coroutine-based C++ networking library focused on HTTP/1 request/response handling, routing, middleware, parsing, and client/server primitives on top of <code>uvent</code>.</p>"},{"location":"#scope-of-this-documentation","title":"Scope Of This Documentation","text":"<p>This documentation intentionally focuses on the parts that are currently practical to use:</p> <ul> <li>HTTP/1 server flow (<code>ServerImpl</code> / <code>ServerRadix</code>)</li> <li>HTTP/1 wire parser and serializer</li> <li>HTTP client (<code>ClientImpl</code>) with plain and TLS streams</li> <li>Router, middleware, headers, and multipart helpers</li> </ul> <p>Out of scope in this docs set:</p> <ul> <li>HTTP/2 internals (still in progress)</li> <li>mail/IMAP model headers (not part of active HTTP runtime)</li> <li>old <code>examples/</code> behavior (mostly legacy)</li> </ul>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Getting started: Installation, Quick Start</li> <li>Runtime setup: Architecture, Configuration</li> <li>Request handling: Routing, Writing Custom Routers, Middlewares, Request and Response, Handlers</li> <li>Outbound requests: HTTP Client</li> <li>Parsing internals: Parsing, Experimental Parse Error Notes</li> <li>Utilities: Multipart Form Data</li> <li>Project process: Roadmap, Contributing</li> </ul>"},{"location":"#current-reality-snapshot","title":"Current Reality Snapshot","text":"<ul> <li>HTTP/1 parser and core flow are the primary implemented path.</li> <li>Middleware phases exist, but runtime invocation details matter (see Middlewares).</li> <li>Tests are present and useful for behavior clues, but they are not exhaustive quality gates yet.</li> <li>API names in this docs match current headers under <code>include/unet/**</code>.</li> </ul>"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#runtime-pieces","title":"Runtime Pieces","text":"<p>The current HTTP runtime is composed of:</p> <ul> <li><code>usub::Uvent</code>: event loop and coroutine scheduler</li> <li><code>usub::unet::http::ServerImpl&lt;Router, Streams...&gt;</code>: acceptor bootstrap and connection loop</li> <li><code>usub::unet::http::router::Radix</code>: route matching, middleware containers, and error handlers</li> <li>stream types such as <code>PlainText</code> and <code>OpenSSLStream</code></li> <li>HTTP session implementation for <code>HTTP/1.1</code></li> </ul>"},{"location":"architecture/#server-lifecycle","title":"Server Lifecycle","text":"<p>Typical startup sequence:</p> <ol> <li>Construct <code>usub::Uvent</code>.</li> <li>Construct a server (<code>ServerRadix</code> or custom <code>ServerImpl&lt;...&gt;</code>).</li> <li>Register routes, middleware, and error handlers.</li> <li>Run <code>uvent</code> with <code>runtime.run()</code>.</li> </ol> <p>There is no <code>server.run()</code> API in the current <code>unet</code> server implementation.</p>"},{"location":"architecture/#connection-pipeline-http1","title":"Connection Pipeline (HTTP/1)","text":"<p>For each accepted socket:</p> <ol> <li><code>Bootstrap</code> receives initial bytes and decides protocol target.</li> <li>For HTTP/1 traffic, session upgrades to <code>ServerSession&lt;VERSION::HTTP_1_1, RouterType&gt;</code>.</li> <li>Request parser consumes bytes incrementally.</li> <li>When headers are complete:</li> <li>route match is attempted</li> <li>global header middleware runs</li> <li>route header middleware runs</li> <li>Body parsing continues (content-length or chunked), body middleware may run.</li> <li>Route handler coroutine executes when request is complete.</li> <li>Response is serialized (<code>v1::ResponseSerializer</code>) and written.</li> <li>Request/response state is reset for potential next request on same connection.</li> </ol>"},{"location":"architecture/#router-and-middleware-placement","title":"Router and Middleware Placement","text":"<ul> <li>Global middleware: <code>Radix::middleware_chain_</code></li> <li>Route middleware: <code>RadixRoute::middleware_chain</code></li> <li>Error handlers: string-keyed (<code>addErrorHandler(level, fn)</code>)</li> </ul>"},{"location":"architecture/#streams-and-acceptors","title":"Streams and Acceptors","text":"<p><code>ServerImpl</code> owns an <code>Acceptor&lt;T&gt;</code> for each stream in template parameters.</p> <ul> <li><code>Acceptor&lt;PlainText&gt;</code> reads <code>HTTP.PlainTextStream</code> config section</li> <li><code>Acceptor&lt;OpenSSLStream&gt;</code> reads <code>HTTP.OpenSSLStream</code> config section</li> </ul> <p>If config is empty, each acceptor falls back to built-in defaults.</p>"},{"location":"client/","title":"HTTP Client","text":"<p>Client API is <code>usub::unet::http::ClientImpl&lt;Streams...&gt;</code>.</p>"},{"location":"client/#core-types","title":"Core Types","text":"<ul> <li><code>ClientImpl&lt;...&gt;</code></li> <li><code>ClientSession&lt;VERSION::HTTP_1_1&gt;</code> (internal protocol session used by the client runtime)</li> <li><code>ClientProxyOptions</code></li> <li><code>ClientRequestOptions</code> (connection timeout)</li> <li><code>ClientError</code></li> </ul> <p><code>request(...)</code> returns:</p> <ul> <li><code>Awaitable&lt;std::expected&lt;Response, ClientError&gt;&gt;</code></li> </ul>"},{"location":"client/#required-request-fields","title":"Required Request Fields","text":"<p>Set at minimum:</p> <ul> <li><code>request.metadata.authority</code> (required)</li> <li><code>request.metadata.uri.authority.host</code></li> </ul> <p>Common explicit fields:</p> <ul> <li><code>method_token</code> (<code>GET</code>, <code>POST</code>, ...)</li> <li><code>uri.scheme</code> (<code>http</code> / <code>https</code>)</li> <li><code>uri.path</code></li> <li><code>uri.authority.port</code></li> </ul>"},{"location":"client/#defaults-applied-by-client","title":"Defaults Applied By Client","text":"<p>If omitted, current client logic defaults:</p> <ul> <li>method -&gt; <code>GET</code></li> <li>version -&gt; <code>HTTP/1.1</code></li> <li>path -&gt; <code>/</code></li> <li>scheme -&gt; inferred from available stream types</li> <li><code>host</code> header -&gt; auto-added when missing and authority host is available</li> <li>HTTP/1.1 connections are kept alive by default when the server allows it</li> <li>HTTP/1.0 connections are only reused when keep-alive is explicitly negotiated</li> <li><code>Keep-Alive: timeout=...</code> is respected by expiring idle client connections before reuse</li> <li>TLS client hostname / SNI is derived from the request host when no explicit managed stream config is provided</li> <li>Optional HTTP proxy support is available for both plain HTTP requests and HTTPS <code>CONNECT</code> tunneling</li> </ul>"},{"location":"client/#persistent-connections","title":"Persistent Connections","text":"<p>The client now keeps one reusable connection per stream type and target endpoint.</p> <ul> <li>Reuse is attempted automatically for HTTP/1.1 unless either side sends <code>Connection: close</code></li> <li>For HTTP/1.0, reuse only happens when <code>Connection: keep-alive</code> is negotiated</li> <li>Responses that are framed <code>until close</code> are never reused</li> <li>If an idle keep-alive timeout has elapsed, the client closes and reconnects before the next request</li> </ul> <p>You can explicitly close any live client-side connections with:</p> <pre><code>co_await client.close();\n</code></pre>"},{"location":"client/#session-structure","title":"Session Structure","text":"<p>The client runtime now delegates HTTP/1.1 response parsing to a dedicated session implementation.</p> <ul> <li><code>ClientImpl</code> owns connection reuse, proxy routing, and stream selection</li> <li><code>ClientSession&lt;VERSION::HTTP_1_1&gt;</code> owns incremental HTTP/1.1 response parsing through <code>onBytes(...)</code> / <code>onClose()</code></li> </ul>"},{"location":"client/#managed-stream-config","title":"Managed Stream Config","text":"<p>For configurable streams such as <code>OpenSSLStream</code>, you can set a default client-managed config once:</p> <pre><code>usub::unet::core::stream::OpenSSLStream::Config tls_cfg{};\ntls_cfg.verify_peer = true;\n\nclient.setStreamConfig&lt;usub::unet::core::stream::OpenSSLStream&gt;(std::move(tls_cfg));\n</code></pre> <p>If <code>server_name</code> is left empty, the client fills it from <code>request.metadata.uri.authority.host</code> when opening a new TLS connection. Prefer <code>client.setStreamConfig&lt;...&gt;(...)</code> for client-side TLS defaults so the client can keep that config aligned with connection reuse.</p>"},{"location":"client/#proxy-support","title":"Proxy Support","text":"<p>Set a proxy per request through <code>ClientRequestOptions</code>:</p> <pre><code>usub::unet::http::ClientRequestOptions options{};\noptions.proxy = usub::unet::http::ClientProxyOptions{\n    .host = \"127.0.0.1\",\n    .port = 8080,\n    .username = \"user\",\n    .password = \"pass\",\n};\n</code></pre> <p>Current client proxy behavior:</p> <ul> <li>Plain HTTP over proxy: sends absolute-form requests, for example <code>GET http://host:port/path HTTP/1.1</code></li> <li>HTTPS over proxy: opens a TCP connection to the proxy, sends <code>CONNECT host:port HTTP/1.1</code>, then runs TLS over the established tunnel</li> <li>If credentials are present, the client sends <code>Proxy-Authorization: Basic ...</code></li> <li>Proxy connections participate in the same client-side persistence logic as direct connections</li> </ul>"},{"location":"client/#plain-tls-example","title":"Plain + TLS Example","text":"<pre><code>using Client = usub::unet::http::ClientImpl&lt;\n    usub::unet::core::stream::PlainText,\n    usub::unet::core::stream::OpenSSLStream\n&gt;;\n\nusub::uvent::task::Awaitable&lt;void&gt; run(Client&amp; client) {\n    usub::unet::http::Request req{};\n    req.metadata.method_token = \"GET\";\n    req.metadata.version = usub::unet::http::VERSION::HTTP_1_1;\n    req.metadata.uri.scheme = \"https\";\n    req.metadata.uri.authority.host = \"example.com\";\n    req.metadata.uri.authority.port = 443;\n    req.metadata.authority = \"example.com\";\n    req.metadata.uri.path = \"/\";\n\n    auto result = co_await client.request(std::move(req));\n    if (!result) {\n        // inspect result.error().code / .message\n        co_return;\n    }\n\n    const auto status = result-&gt;metadata.status_code;\n    const auto&amp; body = result-&gt;body;\n    (void)status;\n    (void)body;\n\n    co_await client.close();\n    co_return;\n}\n</code></pre>"},{"location":"client/#error-codes","title":"Error Codes","text":"<p><code>ClientError::CODE</code> values:</p> <ul> <li><code>INVALID_REQUEST</code></li> <li><code>CONNECT_FAILED</code></li> <li><code>PROXY_FAILED</code></li> <li><code>WRITE_FAILED</code></li> <li><code>READ_FAILED</code></li> <li><code>PARSE_FAILED</code></li> <li><code>CLOSE_FAILED</code></li> </ul> <p>When parse fails, <code>parse_error</code> may include lower-level parse details.</p>"},{"location":"config/","title":"Configuration","text":"<p>Current server configuration is passed as a <code>usub::unet::core::Config</code> object.</p> <p>The old <code>[server]</code> + <code>[[listener]]</code> shape used in legacy docs/examples is not the active shape for the current HTTP server implementation.</p>"},{"location":"config/#active-config-sections","title":"Active Config Sections","text":""},{"location":"config/#httpplaintextstream","title":"<code>HTTP.PlainTextStream</code>","text":"<p>Recognized keys:</p> <ul> <li><code>host</code> (string, default <code>127.0.0.1</code>)</li> <li><code>port</code> (uint, default <code>22813</code>)</li> <li><code>backlog</code> (int, default <code>50</code>)</li> <li><code>version</code> (int, <code>4</code> or <code>6</code>, default <code>4</code>)</li> <li><code>tcp</code> (string, <code>tcp</code> or <code>udp</code>, default <code>tcp</code>)</li> </ul>"},{"location":"config/#httpopensslstream","title":"<code>HTTP.OpenSSLStream</code>","text":"<p>Recognized keys:</p> <ul> <li><code>host</code> (string, default <code>127.0.0.1</code>)</li> <li><code>port</code> (uint, default <code>443</code>)</li> <li><code>backlog</code> (int, default <code>50</code>)</li> <li><code>version</code> (int, <code>4</code> or <code>6</code>, default <code>4</code>)</li> <li><code>tcp</code> (string, <code>tcp</code> or <code>udp</code>, default <code>tcp</code>)</li> <li><code>key</code> (string path, default <code>key.pem</code>)</li> <li><code>cert</code> (string path, default <code>cert.pem</code>)</li> </ul>"},{"location":"config/#minimal-programmatic-config-example","title":"Minimal Programmatic Config Example","text":"<pre><code>#include &lt;unet/core/config.hpp&gt;\n\nusing Cfg = usub::unet::core::Config;\n\nstatic Cfg::Value s(std::string v) { Cfg::Value x; x.data = std::move(v); return x; }\nstatic Cfg::Value u(std::uint64_t v) { Cfg::Value x; x.data = v; return x; }\nstatic Cfg::Value o(Cfg::Object v) { Cfg::Value x; x.data = std::move(v); return x; }\n\nCfg makeConfig() {\n    Cfg cfg;\n\n    Cfg::Object plain;\n    plain.emplace(\"host\", s(\"127.0.0.1\"));\n    plain.emplace(\"port\", u(8080));\n    plain.emplace(\"backlog\", u(128));\n    plain.emplace(\"version\", u(4));\n    plain.emplace(\"tcp\", s(\"tcp\"));\n\n    Cfg::Object tls;\n    tls.emplace(\"host\", s(\"0.0.0.0\"));\n    tls.emplace(\"port\", u(8443));\n    tls.emplace(\"backlog\", u(128));\n    tls.emplace(\"version\", u(4));\n    tls.emplace(\"tcp\", s(\"tcp\"));\n    tls.emplace(\"key\", s(\"server.key\"));\n    tls.emplace(\"cert\", s(\"server.crt\"));\n\n    Cfg::Object http;\n    http.emplace(\"PlainTextStream\", o(std::move(plain)));\n    http.emplace(\"OpenSSLStream\", o(std::move(tls)));\n\n    cfg.root.emplace(\"HTTP\", o(std::move(http)));\n    return cfg;\n}\n</code></pre>"},{"location":"config/#use-with-server","title":"Use With Server","text":"<pre><code>usub::unet::core::Config cfg = makeConfig();\nusub::unet::http::ServerImpl&lt;\n    usub::unet::http::router::Radix,\n    usub::unet::core::stream::PlainText,\n    usub::unet::core::stream::OpenSSLStream\n&gt; server{cfg};\n</code></pre>"},{"location":"config/#notes","title":"Notes","text":"<ul> <li>Missing keys are defaulted by each acceptor implementation.</li> <li>Values are read with <code>Config::getString/getUInt/getInt</code> helpers.</li> <li>Dotted paths are resolved as nested objects (for example <code>HTTP.PlainTextStream</code>).</li> </ul>"},{"location":"contributing/","title":"Contributing","text":""},{"location":"contributing/#high-value-contribution-areas","title":"High-Value Contribution Areas","text":"<ul> <li>HTTP/1 parser correctness and edge cases</li> <li>middleware and routing behavior cleanup</li> <li>client reliability improvements</li> <li>test quality improvements</li> <li>docs updates that match current headers</li> </ul>"},{"location":"contributing/#suggested-workflow","title":"Suggested Workflow","text":"<ol> <li>Open an issue/discussion for non-trivial changes.</li> <li>Implement in small, reviewable commits.</li> <li>Add or update tests for behavior changes.</li> <li>Update docs when public behavior changes.</li> </ol>"},{"location":"contributing/#style-expectations","title":"Style Expectations","text":"<ul> <li>C++23</li> <li>keep code style consistent with nearby files</li> <li>prefer clear parser state transitions over dense logic</li> </ul>"},{"location":"contributing/#reality-check","title":"Reality Check","text":"<p>Current tests are useful but non-exhaustive. Add targeted regression coverage when fixing bugs.</p>"},{"location":"contributing/#links","title":"Links","text":"<ul> <li>Issues: <code>https://github.com/Usub-development/unet/issues</code></li> <li>Discussions: <code>https://github.com/Usub-development/unet/discussions</code></li> <li>Pull requests: <code>https://github.com/Usub-development/unet/pulls</code></li> </ul>"},{"location":"custom-router/","title":"Writing Custom Routers","text":"<p>You can plug your own router into <code>ServerImpl</code> by replacing <code>router::Radix</code> with your own type.</p>"},{"location":"custom-router/#where-it-is-used","title":"Where It Is Used","text":"<p><code>ServerImpl&lt;RouterType, ...&gt;</code> and <code>ServerSession&lt;VERSION::HTTP_1_1, RouterType&gt;</code> call router methods directly. So your router must provide a compatible API surface.</p>"},{"location":"custom-router/#required-router-interface","title":"Required Router Interface","text":"<p>Your router type must provide:</p> <ul> <li><code>using MatchResult = ...;</code></li> <li><code>addRoute(...)</code> (used by <code>server.handle(...)</code>)</li> <li><code>addMiddleware(MIDDLEWARE_PHASE, std::function&lt;MiddlewareFunctionType&gt;)</code></li> <li><code>addErrorHandler(const std::string&amp;, std::function&lt;void(const Request&amp;, Response&amp;)&gt;)</code></li> <li><code>error(const std::string&amp;, const Request&amp;, Response&amp;)</code></li> <li><code>match(const Request&amp;) -&gt; std::expected&lt;MatchResult, STATUS_CODE&gt;</code></li> <li><code>getMiddlewareChain() -&gt; MiddlewareChain&amp;</code></li> <li><code>runRouteMiddleware(MIDDLEWARE_PHASE, MatchResult&amp;, Request&amp;, Response&amp;) -&gt; bool</code></li> <li><code>invoke(MatchResult&amp;, Request&amp;, Response&amp;) -&gt; Awaitable&lt;void&gt;</code></li> </ul>"},{"location":"custom-router/#minimal-skeleton","title":"Minimal Skeleton","text":"<pre><code>#include &lt;expected&gt;\n#include &lt;functional&gt;\n#include &lt;string&gt;\n#include &lt;unordered_map&gt;\n\n#include \"unet/http/core/request.hpp\"\n#include \"unet/http/core/response.hpp\"\n#include \"unet/http/middleware.hpp\"\n\nnamespace usub::unet::http::router {\n    class MyRouter {\n    public:\n        struct MatchResult {\n            // put route-specific match context here\n        };\n\n        template&lt;typename... Args&gt;\n        auto &amp;addRoute(Args &amp;&amp;... /*unused*/) {\n            // register route, return route object if you want chaining\n            return *this;\n        }\n\n        MyRouter &amp;addMiddleware(MIDDLEWARE_PHASE phase, std::function&lt;MiddlewareFunctionType&gt; middleware) {\n            this-&gt;middleware_chain_.addMiddleware(phase, std::move(middleware));\n            return *this;\n        }\n\n        MyRouter &amp;addErrorHandler(const std::string &amp;level, std::function&lt;void(const Request &amp;, Response &amp;)&gt; fn) {\n            this-&gt;error_handlers_[level] = std::move(fn);\n            return *this;\n        }\n\n        void error(const std::string &amp;level, const Request &amp;req, Response &amp;res) {\n            if (auto it = this-&gt;error_handlers_.find(level); it != this-&gt;error_handlers_.end()) {\n                it-&gt;second(req, res);\n            }\n        }\n\n        std::expected&lt;MatchResult, STATUS_CODE&gt; match(const Request &amp;request) {\n            (void) request;\n            return MatchResult{};\n        }\n\n        MiddlewareChain &amp;getMiddlewareChain() { return this-&gt;middleware_chain_; }\n\n        bool runRouteMiddleware(MIDDLEWARE_PHASE phase, MatchResult &amp;match, Request &amp;req, Response &amp;res) {\n            (void) match;\n            return this-&gt;middleware_chain_.execute(phase, req, res);\n        }\n\n        usub::uvent::task::Awaitable&lt;void&gt; invoke(MatchResult &amp;match, Request &amp;req, Response &amp;res) {\n            (void) match;\n            (void) req;\n            (void) res;\n            co_return;\n        }\n\n    private:\n        MiddlewareChain middleware_chain_{};\n        std::unordered_map&lt;std::string, std::function&lt;void(const Request &amp;, Response &amp;)&gt;&gt; error_handlers_{};\n    };\n}// namespace usub::unet::http::router\n</code></pre>"},{"location":"custom-router/#use-it-in-server","title":"Use It In Server","text":"<pre><code>using MyServer = usub::unet::http::ServerImpl&lt;\n    usub::unet::http::router::MyRouter,\n    usub::unet::core::stream::PlainText\n&gt;;\n</code></pre>"},{"location":"custom-router/#practical-advice","title":"Practical Advice","text":"<ul> <li>Start by copying <code>router::Radix</code> behavior that your app needs.</li> <li>Keep <code>MatchResult</code> lightweight; it is stored in session state.</li> <li>If you support route-level middleware, ensure <code>runRouteMiddleware(...)</code> uses the route matched in <code>match(...)</code>.</li> </ul>"},{"location":"handler/","title":"Handlers","text":"<p>A route handler is a coroutine that returns <code>Awaitable&lt;void&gt;</code>.</p>"},{"location":"handler/#canonical-shape","title":"Canonical Shape","text":"<pre><code>usub::uvent::task::Awaitable&lt;void&gt;\nhandler(usub::unet::http::Request&amp; req, usub::unet::http::Response&amp; res) {\n    (void)req;\n    res.setStatus(200).setBody(\"ok\\n\");\n    co_return;\n}\n</code></pre>"},{"location":"handler/#with-route-params","title":"With Route Params","text":"<p>If your route has URI params, accept them as a third argument:</p> <pre><code>usub::uvent::task::Awaitable&lt;void&gt;\nhandler(usub::unet::http::Request&amp;, usub::unet::http::Response&amp; res,\n        const usub::unet::http::router::RadixMatch::UriParams&amp; params) {\n    auto id = params.contains(\"id\") ? params.at(\"id\") : \"missing\";\n    res.setStatus(200).setBody(\"id=\" + id + \"\\n\");\n    co_return;\n}\n</code></pre>"},{"location":"handler/#binding-class-member-handlers","title":"Binding Class Member Handlers","text":"<p>You can register a class member function by binding an instance first.</p> <pre><code>class AdminController {\npublic:\n    usub::uvent::task::Awaitable&lt;void&gt;\n    status(usub::unet::http::Request&amp; req, usub::unet::http::Response&amp; res) {\n        (void)req;\n        res.setStatus(200).setBody(\"admin ok\\n\");\n        co_return;\n    }\n};\n\nint main() {\n    usub::Uvent runtime{2};\n    usub::unet::http::ServerRadix server{runtime};\n\n    AdminController controller{};\n    server.handle(\"GET\", \"/admin/status\",\n                  std::bind_front(&amp;AdminController::status, &amp;controller));\n\n    runtime.run();\n}\n</code></pre> <p>For member handlers with URI params, use the same pattern: <code>std::bind_front(&amp;Controller::method, &amp;controller_instance)</code>.</p>"},{"location":"handler/#practical-rules","title":"Practical Rules","text":"<ul> <li>keep handlers non-blocking</li> <li>set status/headers/body explicitly</li> <li>use middleware for cross-cutting concerns</li> </ul>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Linux environment (current project target)</li> <li>C++23 compiler</li> <li>CMake 3.22+</li> <li>Git</li> </ul> <p>Optional:</p> <ul> <li>OpenSSL (required for TLS stream features/tests)</li> </ul>"},{"location":"installation/#build-from-source","title":"Build From Source","text":"<pre><code>git clone https://github.com/Usub-development/unet.git\ncd unet\ncmake -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build -j\n</code></pre>"},{"location":"installation/#install","title":"Install","text":"<pre><code>cmake --install build\n</code></pre> <p>By default, headers are installed under your CMake install prefix (for example <code>/usr/local/include</code>) and CMake package files are installed under <code>lib/cmake/unet</code>.</p>"},{"location":"installation/#use-from-another-cmake-project","title":"Use From Another CMake Project","text":"<pre><code>cmake_minimum_required(VERSION 3.22)\nproject(my_app LANGUAGES CXX)\n\nfind_package(unet REQUIRED)\n\nadd_executable(my_app main.cpp)\ntarget_link_libraries(my_app PRIVATE usub::unet)\n</code></pre>"},{"location":"installation/#fetchcontent-development","title":"FetchContent (Development)","text":"<pre><code>include(FetchContent)\n\nFetchContent_Declare(\n  unet\n  GIT_REPOSITORY https://github.com/Usub-development/unet.git\n  GIT_TAG main\n)\n\nFetchContent_MakeAvailable(unet)\n\nadd_executable(my_app main.cpp)\ntarget_link_libraries(my_app PRIVATE usub::unet)\n</code></pre>"},{"location":"installation/#build-options-current-state","title":"Build Options (Current State)","text":"<p>Declared options in <code>CMakeLists.txt</code>:</p> <ul> <li><code>UNET_BUILD_EXAMPLES</code> (default <code>OFF</code>)</li> <li><code>UNET_BUILD_TESTS</code> (default <code>ON</code>)</li> </ul> <p>Current top-level conditions still check <code>BUILD_EXAMPLES</code> for enabling subdirectories, so if you want examples/tests added by this file as-is, use:</p> <pre><code>cmake -B build -DBUILD_EXAMPLES=ON\n</code></pre>"},{"location":"installation/#generate-documentation-site","title":"Generate Documentation Site","text":"<p>If MkDocs is installed:</p> <pre><code>mkdocs build -f docs/mkdocs.yml\n# or\nmkdocs serve -f docs/mkdocs.yml\n</code></pre>"},{"location":"middlewares/","title":"Middlewares","text":"<p>Middleware functions intercept request/response handling phases.</p>"},{"location":"middlewares/#signature","title":"Signature","text":"<pre><code>bool(Request&amp;, Response&amp;)\n</code></pre> <p>Return value:</p> <ul> <li><code>true</code>: continue processing</li> <li><code>false</code>: stop middleware/handler flow for that phase chain</li> </ul>"},{"location":"middlewares/#phases","title":"Phases","text":"<p><code>MIDDLEWARE_PHASE</code> values:</p> <ul> <li><code>HEADER</code></li> <li><code>BODY</code></li> <li><code>RESPONSE</code></li> </ul>"},{"location":"middlewares/#registering-middleware","title":"Registering Middleware","text":"<p>Global middleware:</p> <pre><code>server.addMiddleware(usub::unet::http::MIDDLEWARE_PHASE::HEADER, globalHeaderMw);\n</code></pre> <p>Route middleware:</p> <pre><code>server.handle(\"GET\", \"/hello\", handler)\n      .addMiddleware(usub::unet::http::MIDDLEWARE_PHASE::HEADER, routeHeaderMw)\n      .addMiddleware(usub::unet::http::MIDDLEWARE_PHASE::BODY, routeBodyMw);\n</code></pre>"},{"location":"middlewares/#current-runtime-behavior-important","title":"Current Runtime Behavior (Important)","text":"<p>In current <code>HTTP/1.1</code> session logic:</p> <ul> <li>header middleware is executed</li> <li>body middleware is executed</li> <li>response middleware is defined in API but is not currently invoked in the HTTP/1 send path</li> </ul> <p>Global middleware limitation in current radix implementation:</p> <ul> <li><code>Radix::addMiddleware(...)</code> supports global <code>HEADER</code> phase only</li> <li>non-header global registration logs a warning and is ignored</li> </ul>"},{"location":"middlewares/#request-scoped-user-data","title":"Request-Scoped User Data","text":"<p><code>Request</code> includes:</p> <pre><code>std::any user_data;\n</code></pre> <p>Use this to pass context between middleware and handlers during one request lifecycle.</p>"},{"location":"multipart-form-data/","title":"Multipart Form Data","text":"<p>Multipart parser utility lives in:</p> <ul> <li><code>unet/mime/multipart/form_data/generic.hpp</code></li> </ul> <p>Main type: <code>usub::unet::mime::multipart::FormData</code></p>"},{"location":"multipart-form-data/#basic-usage","title":"Basic Usage","text":"<pre><code>using usub::unet::mime::multipart::FormData;\n\nstd::string boundary = \"----MyBoundary\";\nstd::string body = /* raw multipart payload */;\n\nFormData form(boundary);\nauto result = form.parse(body);\nif (!result) {\n    // result.error() has an error string\n}\n\nif (form.contains(\"file\")) {\n    const auto&amp; parts = form.at(\"file\");\n    // parts[i].data, parts[i].content_type, parts[i].disposition\n}\n</code></pre>"},{"location":"multipart-form-data/#parsed-part-structure","title":"Parsed Part Structure","text":"<p>Each <code>Part</code> includes:</p> <ul> <li><code>content_type</code></li> <li><code>disposition</code> map</li> <li><code>data</code></li> <li>extra <code>headers</code> map</li> </ul> <p>Parts are indexed by <code>name</code> from <code>Content-Disposition</code>.</p>"},{"location":"multipart-form-data/#notes","title":"Notes","text":"<ul> <li>boundary must be provided explicitly</li> <li>parsing is whole-buffer (not streaming)</li> <li>error type is <code>std::expected&lt;void, std::string&gt;</code></li> </ul>"},{"location":"parsing/","title":"Parsing","text":"<p>HTTP/1 parsing is implemented as incremental state machines.</p>"},{"location":"parsing/#request-parsing","title":"Request Parsing","text":"<p>Type: <code>usub::unet::http::v1::RequestParser</code></p> <p>Primary APIs:</p> <ul> <li><code>static parse(std::string_view)</code> for one-shot parse</li> <li><code>step(Request&amp;, begin, end)</code> for incremental parse</li> </ul> <p>Parser emits <code>ParseStep</code> kinds:</p> <ul> <li><code>CONTINUE</code></li> <li><code>HEADERS</code></li> <li><code>BODY</code></li> <li><code>COMPLETE</code></li> </ul>"},{"location":"parsing/#validation-highlights","title":"Validation Highlights","text":"<p>Current request parser behavior includes:</p> <ul> <li>requires <code>Host</code> header for HTTP/1.1 flow</li> <li>validates method token and version token</li> <li>supports origin-form URI (<code>/path?query</code>)</li> <li>rejects URI fragments in request target</li> <li>supports content-length and chunked parsing</li> <li>validates conflicting or invalid <code>Content-Length</code></li> <li>enforces method/URI/header limits via global settings</li> </ul>"},{"location":"parsing/#response-parsing","title":"Response Parsing","text":"<p>Type: <code>usub::unet::http::v1::ResponseParser</code></p> <p>Supports:</p> <ul> <li>status line parsing</li> <li>header parsing and validation</li> <li>body framing by content-length, chunked, or until-close mode</li> </ul> <p>Used directly by <code>ClientImpl</code> to decode responses.</p>"},{"location":"parsing/#parse-errors","title":"Parse Errors","text":"<p>Common error container:</p> <pre><code>struct ParseError {\n    CODE code;\n    STATUS_CODE expected_status;\n    std::string message;\n    std::array&lt;char, 256&gt; tail;\n};\n</code></pre> <p>For handling guidance, see Experimental Parse Error Notes.</p>"},{"location":"parsing/#coverage-note","title":"Coverage Note","text":"<p>Tests under <code>tests/</code> provide behavior coverage signals, but they are not exhaustive.</p>"},{"location":"quick-start/","title":"Quick Start","text":"<p>This page shows the shortest working server flow with the current API.</p>"},{"location":"quick-start/#1-minimal-http-server","title":"1. Minimal HTTP Server","text":"<pre><code>#include &lt;uvent/Uvent.h&gt;\n#include &lt;unet/http.hpp&gt;\n\nusing usub::unet::http::Request;\nusing usub::unet::http::Response;\n\nusub::uvent::task::Awaitable&lt;void&gt; hello(Request&amp;, Response&amp; res) {\n    res.setStatus(usub::unet::http::STATUS_CODE::OK)\n       .addHeader(\"content-type\", \"text/plain\")\n       .setBody(\"Hello from unet\\n\");\n    co_return;\n}\n\nint main() {\n    usub::Uvent runtime{4};\n\n    usub::unet::http::ServerRadix server;\n    server.handle(\"GET\", \"/hello\", hello);\n\n    runtime.run();\n    return 0;\n}\n</code></pre> <p>Default <code>ServerRadix</code> listener config (when no explicit config is provided):</p> <ul> <li>Host: <code>127.0.0.1</code></li> <li>Port: <code>22813</code></li> <li>Backlog: <code>50</code></li> </ul>"},{"location":"quick-start/#2-build","title":"2. Build","text":"<pre><code>cmake -B build -DCMAKE_BUILD_TYPE=Debug\ncmake --build build -j\n</code></pre>"},{"location":"quick-start/#3-test","title":"3. Test","text":"<pre><code>curl -i http://127.0.0.1:22813/hello\n</code></pre> <p>Expected status: <code>200 OK</code></p>"},{"location":"quick-start/#4-next-steps","title":"4. Next Steps","text":"<ul> <li>Configuration</li> <li>Routing</li> <li>Middlewares</li> <li>Request and Response</li> </ul>"},{"location":"request-response/","title":"Request and Response","text":""},{"location":"request-response/#request-model","title":"Request Model","text":"<p>Main type: <code>usub::unet::http::Request</code></p> <p>Key fields:</p> <ul> <li><code>metadata.method_token</code></li> <li><code>metadata.version</code></li> <li><code>metadata.uri</code> (<code>scheme</code>, <code>authority</code>, <code>path</code>, <code>query</code>, <code>fragment</code>)</li> <li><code>metadata.authority</code></li> <li><code>headers</code> (<code>usub::unet::header::Headers</code>)</li> <li><code>body</code></li> <li><code>user_data</code> (<code>std::any</code>)</li> </ul>"},{"location":"request-response/#query-convenience","title":"Query Convenience","text":"<p><code>Request::getQueryAs&lt;T&gt;()</code> currently returns from <code>metadata.uri.query</code>.</p>"},{"location":"request-response/#response-model","title":"Response Model","text":"<p>Main type: <code>usub::unet::http::Response</code></p> <p>Key fields:</p> <ul> <li><code>metadata.version</code></li> <li><code>metadata.status_code</code></li> <li><code>metadata.status_message</code> (optional override)</li> <li><code>headers</code></li> <li><code>body</code></li> </ul>"},{"location":"request-response/#fluent-helpers","title":"Fluent Helpers","text":"<pre><code>res.setStatus(200)\n   .addHeader(\"content-type\", \"application/json\")\n   .setBody(\"{\\\"ok\\\":true}\");\n</code></pre> <p>Notes:</p> <ul> <li><code>setBody(...)</code> updates <code>content-length</code></li> <li><code>setBody(...)</code> does not automatically set <code>content-type</code></li> <li><code>setStatus(std::string)</code> exists but is deprecated</li> </ul>"},{"location":"request-response/#headers-container","title":"Headers Container","text":"<p><code>usub::unet::header::Headers</code> behavior:</p> <ul> <li>header names are normalized to lowercase</li> <li>multiple values per key are supported</li> <li>common methods: <code>addHeader</code>, <code>contains</code>, <code>first</code>, <code>all(key)</code>, <code>value(key)</code></li> </ul>"},{"location":"request-response/#http-status-codes","title":"HTTP Status Codes","text":"<p><code>STATUS_CODE</code> enum includes common RFC and extended values (for example <code>OK</code>, <code>NOT_FOUND</code>, <code>INTERNAL_SERVER_ERROR</code>).</p>"},{"location":"request-response/#parser-limits-globals","title":"Parser Limits (Globals)","text":"<ul> <li><code>max_headers_size</code> (default <code>256 * 1024</code>)</li> <li><code>max_method_token_size</code> (default <code>uint8_t</code> max)</li> <li><code>max_uri_size</code> (default <code>uint16_t</code> max)</li> </ul>"},{"location":"roadmap/","title":"Roadmap","text":""},{"location":"roadmap/#near-term","title":"Near-Term","text":"<ul> <li>tighten HTTP/1 parser edge-case coverage</li> <li>improve middleware phase consistency (including response phase execution)</li> <li>simplify server configuration ergonomics</li> <li>improve test determinism and quality</li> </ul>"},{"location":"roadmap/#mid-term","title":"Mid-Term","text":"<ul> <li>stronger client coverage (timeouts, malformed responses, TLS edges)</li> <li>clearer public API docs around route constraints and handler overloads</li> <li>packaging and CMake polish</li> </ul>"},{"location":"roadmap/#long-term","title":"Long-Term","text":"<ul> <li>protocol expansion after HTTP/1 core stability</li> <li>benchmark and profiling documentation</li> </ul>"},{"location":"roadmap/#out-of-scope-in-this-cycle","title":"Out Of Scope In This Cycle","text":"<ul> <li>documenting unfinished HTTP/2 internals</li> <li>expanding docs for non-HTTP experimental modules</li> </ul>"},{"location":"routing/","title":"Routing","text":"<p>Routing is implemented by <code>usub::unet::http::router::Radix</code>.</p>"},{"location":"routing/#registering-routes","title":"Registering Routes","text":"<p>From server:</p> <pre><code>server.handle(\"GET\", \"/health\", handler);\nserver.handle(std::set&lt;std::string&gt;{\"GET\", \"POST\"}, \"/items\", handler);\nserver.handle(std::set&lt;std::string&gt;{\"*\"}, \"/debug\", handler);\n</code></pre> <p>The method wildcard is the literal <code>\"*\"</code> token.</p>"},{"location":"routing/#path-pattern-features","title":"Path Pattern Features","text":"<p>Supported route path shapes:</p> <ul> <li>literal: <code>/health</code></li> <li>named param: <code>/users/{id}</code></li> <li>param with inline regex: <code>/orders/{id:[0-9]+}</code></li> <li>wildcard tail: <code>/files/*</code></li> </ul> <p>Trailing slash is significant (<code>/users/{id}</code> and <code>/users/{id}/</code> are different routes).</p>"},{"location":"routing/#param-constraints","title":"Param Constraints","text":"<p>You can pass a constraints map to overloads that accept it.</p> <p><code>param_constraint</code> type:</p> <pre><code>struct param_constraint {\n    std::string pattern;\n    std::string description;\n};\n</code></pre> <p>If no inline regex and no constraint is provided, default param regex is <code>[^/]+</code>.</p>"},{"location":"routing/#handler-signatures","title":"Handler Signatures","text":"<p><code>RadixRoute::makeHandler(...)</code> accepts multiple forms and adapts them to a unified internal signature.</p> <p>Supported forms:</p> <ul> <li><code>Awaitable&lt;void&gt;(Request&amp;, Response&amp;)</code></li> <li><code>Awaitable&lt;void&gt;(Request&amp;, Response&amp;, RadixMatch&amp;)</code></li> <li><code>Awaitable&lt;void&gt;(Request&amp;, Response&amp;, RadixMatch::UriParams&amp;)</code></li> <li><code>Awaitable&lt;void&gt;(Request&amp;, Response&amp;, const RadixMatch::UriParams&amp;)</code></li> </ul> <p>For class-member handlers, see Handlers.</p>"},{"location":"routing/#accessing-uri-params","title":"Accessing URI Params","text":"<pre><code>usub::uvent::task::Awaitable&lt;void&gt;\ngetUser(usub::unet::http::Request&amp;, usub::unet::http::Response&amp; res,\n        const usub::unet::http::router::RadixMatch::UriParams&amp; params) {\n    auto it = params.find(\"id\");\n    const std::string id = (it == params.end()) ? \"missing\" : it-&gt;second;\n    res.setStatus(200).setBody(\"id=\" + id + \"\\n\");\n    co_return;\n}\n</code></pre> <p>You can also accept <code>RadixMatch&amp;</code> directly and use <code>match.param(\"name\")</code>.</p>"},{"location":"routing/#error-handlers","title":"Error Handlers","text":"<p>Register error callbacks by string key:</p> <pre><code>server.addErrorHandler(\"log\", [](const Request&amp;, Response&amp;) {\n    // logging or metrics\n});\n\nserver.addErrorHandler(\"404\", [](const Request&amp;, Response&amp; res) {\n    res.setStatus(404).setBody(\"not found\\n\");\n});\n</code></pre> <p>The runtime calls keys such as <code>\"log\"</code> and status string values (for example <code>\"404\"</code>).</p>"},{"location":"Experimental/parse_error_handling/","title":"Experimental Parse Error Handling Notes","text":"<p>This page summarizes practical parse-error handling for current HTTP/1 parser APIs.</p>"},{"location":"Experimental/parse_error_handling/#what-you-receive","title":"What You Receive","text":"<p>On parser failure (<code>RequestParser</code> / <code>ResponseParser</code>), you receive <code>ParseError</code> with:</p> <ul> <li><code>expected_status</code>: suggested HTTP status for malformed input</li> <li><code>message</code>: parser failure reason</li> <li><code>tail</code>: reserved buffer (currently lightly used)</li> </ul>"},{"location":"Experimental/parse_error_handling/#practical-server-side-strategy","title":"Practical Server-Side Strategy","text":"<ol> <li>Set <code>response.metadata.status_code</code> from <code>expected_status</code>.</li> <li>Log parser message and relevant request metadata.</li> <li>Treat partially parsed requests as invalid and stop processing.</li> </ol>"},{"location":"Experimental/parse_error_handling/#practical-client-side-strategy","title":"Practical Client-Side Strategy","text":"<p>When <code>ClientImpl::request(...)</code> returns <code>PARSE_FAILED</code>:</p> <ol> <li>Log <code>ClientError.message</code>.</li> <li>If present, log <code>ClientError.parse_error-&gt;message</code>.</li> <li>Treat the response as unusable and retry only when method semantics are safe.</li> </ol>"},{"location":"Experimental/parse_error_handling/#operational-advice","title":"Operational Advice","text":"<ul> <li>track parse error rates by peer/service</li> <li>alert on sustained increases</li> <li>keep raw byte capture behind debug flags and privacy controls</li> </ul>"}]}